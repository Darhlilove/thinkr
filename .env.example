# Google Cloud Configuration
PROJECT="your-gcp-project-id"
DATASET="your-bigquery-dataset-name"
TABLE="your-bigquery-table-name"
REGION="us-central1"

# Google AI API Key
GOOGLE_API_KEY="your-google-api-key"

# Instructions:
# 1. Copy this file to backend/.env (for Python backend)
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit the actual .env files to version control
#
# Note: The Next.js frontend doesn't need environment variables in the current
# hybrid architecture since it only communicates with the FastAPI backend,
# which handles all Google Cloud API interactions.
